const searchEl = document.getElementById("search-btn");

// get roommate name fetch request

// grab roommate post

searchEl.addEventListener("click", function () {
  const searchTerm = inputEl.value;
  // render roommate post
});
